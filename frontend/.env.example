# Optional — Vite dev only. Production API URL is hardcoded in src/services/api.ts (REMOTE_API_ORIGIN).

# Local dev: where Vite proxies /api (default http://127.0.0.1:8000)
# VITE_BACKEND_URL=http://127.0.0.1:8000

# VITE_DEV_PORT=3000
