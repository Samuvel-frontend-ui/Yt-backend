# Optional — Vite dev only. Production calls Render directly (see src/services/api.ts).

# Local dev: where Vite proxies /api (default http://127.0.0.1:8000)
# VITE_BACKEND_URL=http://127.0.0.1:8000

# VITE_DEV_PORT=3000
